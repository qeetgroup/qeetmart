spring:
  datasource:
    url: jdbc:h2:mem:userdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-service.test

app:
  security:
    jwt:
      secret: dGVzdC11c2VyLXNlY3JldC0xMjM0NTY3ODkwMTIzNDU2Nzg5MDE=
      issuer: http://auth-service.test
