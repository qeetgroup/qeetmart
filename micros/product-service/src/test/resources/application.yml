spring:
  datasource:
    url: jdbc:h2:mem:productdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-service.test

app:
  security:
    jwt:
      secret: dGVzdC1wcm9kdWN0LXNlY3JldC0xMjM0NTY3ODkwMTIzNDU2Nzg5MDE=
      issuer: http://auth-service.test
